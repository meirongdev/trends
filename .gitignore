# Go
trends.db
trends.db-*
*.db
*.db-shm
*.db-wal
/trends
bin/

# env
.env

# web (frontend)
web/node_modules/
web/dist/

# embedded SPA build output: ignore everything except the committed placeholder index.html.
# `make web` overwrites this dir with the real Vite build (embedded into the binary at build time).
internal/api/dist/*
!internal/api/dist/index.html

# Go test / coverage / profiling artifacts
*.test
*.out
coverage.*
*.prof

# OS files
.DS_Store
Thumbs.db

# editor / IDE
.idea/
*.swp
*~

# local scratch (throwaway experiments, e.g. one-off validation programs)
scratch*/
